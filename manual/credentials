Administrator
=============

	username : root
	password : admin


Handlers
============

	On a terminal, run : "sudo mysql -u root -p"
	"SELECT * FROM handlers;"

	This way you will be able to see a table, and copy and paste handler usernames
	and passwords in the handler login field.

Users
==========

	On a terminal, run : "sudo mysql -u root -p"
	"SELECT * FROM user;"

	You can now see the user's usernames and passwords, and login as them through
	the login page.